# Temporary directory for map generation (generate_missing_maps.py)

.mapgen-tmp/

# node-mapgen dependencies

node-mapgen/node_modules/

# macOS filesystem junk

.DS_Store
.DS_Store?
.AppleDouble
.LSOverride
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Finder metadata

Icon?
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# Jekyll build output

_site

# Obsidian editor config
.obsidian/

# node-mapgen-broken dependencies
node-mapgen-broken/node_modules/

# Scratch/dump files
file_list*.txt

# Admin app runtime (disposable SQLite index, backups, staged uploads) + venv
admin/.runtime/
admin/.venv/

# node-mapgen's technical-metadata sidecar — disposable, regenerated by every
# mapgen run; the front-matter fields it gets folded into are the source of truth
assets/worldfiles/*/meta.json
__pycache__/
*.pyc
.pytest_cache/

# Secrets / credentials (defensive — none currently exist in the repo)
.env
.env.*
*.pem
*.key
credentials.json
secrets.yaml
